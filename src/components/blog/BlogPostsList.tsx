import { ReactElement } from 'react';
import { APICallerProps } from '../../types/blog.types';

import './BlogPostsList.css';

function BlogPostsList({ token }: APICallerProps): ReactElement {
  return <div>Blogs list page</div>;
}

export { BlogPostsList };
